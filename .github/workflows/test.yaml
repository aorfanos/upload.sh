---
on:
  push:
    branches:
      - master

name: Purge cache
jobs:
  deploy_dev:
    runs-on: ubuntu-latest
    steps:
          
    - name: Purge cache
      uses: aorfanos/cloudflare-purge-cache-action@master
      with:
        authToken: ${{ secrets.CLOUDFLARE_API_KEY }}
        siteName: ${{ secrets.CF_ZONE_NAME }}
        authEmail: ${{ secrets.CF_EMAIL_ADDR}}
#        fileList: "[\"https://aorfanos.com/index.html\",\"http://somesite.com/cat-picture.jpg\"]"
